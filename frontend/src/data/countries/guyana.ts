/**
 * Guyana country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const guyana: Country = { code: 'GY', name: 'Guyana', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'LATAM' }};

export default guyana;
