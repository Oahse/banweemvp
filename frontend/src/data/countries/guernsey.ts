/**
 * Guernsey country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const guernsey: Country = { code: 'GG', name: 'Guernsey', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'EU' }};

export default guernsey;
