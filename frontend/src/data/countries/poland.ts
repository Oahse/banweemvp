/**
 * Poland country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const poland: Country = { code: 'PL', name: 'Poland', taxInfo: { standardRate: 23, taxName: 'VAT', currency: 'PLN', region: 'EU' } };

export default poland;
