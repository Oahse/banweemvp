/**
 * Luxembourg country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const luxembourg: Country = { code: 'LU', name: 'Luxembourg', taxInfo: { standardRate: 17, taxName: 'VAT', currency: 'EUR', region: 'EU' } };

export default luxembourg;
