/**
 * Nauru country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const nauru: Country = { code: 'NR', name: 'Nauru', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'APAC' }};

export default nauru;
