/**
 * Russia country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const russia: Country = { code: 'RU', name: 'Russia', taxInfo: { standardRate: 20, taxName: 'VAT', currency: 'RUB', region: 'EU' } };

export default russia;
