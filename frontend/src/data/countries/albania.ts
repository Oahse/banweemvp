/**
 * Albania country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const albania: Country = { code: 'AL', name: 'Albania', taxInfo: { standardRate: 20, taxName: 'VAT', currency: 'ALL', region: 'EU' } };

export default albania;
