/**
 * Oman country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const oman: Country = { code: 'OM', name: 'Oman', taxInfo: { standardRate: 5, taxName: 'VAT', currency: 'OMR', region: 'MEA' } };

export default oman;
