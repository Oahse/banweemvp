/**
 * Portugal country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const portugal: Country = { code: 'PT', name: 'Portugal', taxInfo: { standardRate: 23, taxName: 'IVA', currency: 'EUR', region: 'EU' } };

export default portugal;
