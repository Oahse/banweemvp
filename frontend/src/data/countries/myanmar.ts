/**
 * Myanmar country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const myanmar: Country = { code: 'MM', name: 'Myanmar', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'APAC' }};

export default myanmar;
