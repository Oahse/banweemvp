/**
 * East Timor country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const easttimor: Country = { code: 'TL', name: 'East Timor', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'APAC' }};

export default easttimor;
