/**
 * Guam country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const guam: Country = { code: 'GU', name: 'Guam', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'APAC' }};

export default guam;
