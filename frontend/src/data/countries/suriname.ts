/**
 * Suriname country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const suriname: Country = { code: 'SR', name: 'Suriname', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'LATAM' }};

export default suriname;
