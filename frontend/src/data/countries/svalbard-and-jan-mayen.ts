/**
 * Svalbard and Jan Mayen country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const svalbardandjanmayen: Country = { code: 'SJ', name: 'Svalbard and Jan Mayen', taxInfo: { taxName: 'No VAT', currency: 'NOK', region: 'EU' }};

export default svalbardandjanmayen;
