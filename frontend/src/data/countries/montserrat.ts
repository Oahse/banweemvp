/**
 * Montserrat country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const montserrat: Country = { code: 'MS', name: 'Montserrat', taxInfo: { taxName: 'No VAT', currency: 'XCD', region: 'NA' }};

export default montserrat;
