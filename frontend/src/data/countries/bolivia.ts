/**
 * Bolivia country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const bolivia: Country = { code: 'BO', name: 'Bolivia', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'LATAM' }};

export default bolivia;
