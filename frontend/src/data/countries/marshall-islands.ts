/**
 * Marshall Islands country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const marshallislands: Country = { code: 'MH', name: 'Marshall Islands', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'APAC' }};

export default marshallislands;
