/**
 * Armenia country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const armenia: Country = { code: 'AM', name: 'Armenia', taxInfo: { standardRate: 20, taxName: 'VAT', currency: 'AMD', region: 'MEA' } };

export default armenia;
