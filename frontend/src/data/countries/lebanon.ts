/**
 * Lebanon country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const lebanon: Country = { code: 'LB', name: 'Lebanon', taxInfo: { standardRate: 11, taxName: 'VAT', currency: 'LBP', region: 'MEA' } };

export default lebanon;
