/**
 * Saint Pierre and Miquelon country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const saintpierreandmiquelon: Country = { code: 'PM', name: 'Saint Pierre and Miquelon', taxInfo: { standardRate: 20, taxName: 'VAT', currency: 'EUR', region: 'NA' };

export default saintpierreandmiquelon;
