/**
 * Lithuania country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const lithuania: Country = { code: 'LT', name: 'Lithuania', taxInfo: { standardRate: 21, taxName: 'VAT', currency: 'EUR', region: 'EU' } };

export default lithuania;
