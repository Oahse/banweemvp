/**
 * Réunion country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const runion: Country = { code: 'RE', name: 'Réunion', taxInfo: { standardRate: 20, taxName: 'VAT', currency: 'EUR', region: 'MEA' };

export default runion;
