/**
 * Singapore country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const singapore: Country = { code: 'SG', name: 'Singapore', taxInfo: { standardRate: 8, taxName: 'GST', currency: 'SGD', region: 'APAC' } };

export default singapore;
