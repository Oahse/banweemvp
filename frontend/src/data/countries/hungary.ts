/**
 * Hungary country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const hungary: Country = { code: 'HU', name: 'Hungary', taxInfo: { standardRate: 27, taxName: 'VAT', currency: 'HUF', region: 'EU' } };

export default hungary;
