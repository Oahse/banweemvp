/**
 * Jordan country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const jordan: Country = { code: 'JO', name: 'Jordan', taxInfo: { standardRate: 16, taxName: 'VAT', currency: 'JOD', region: 'MEA' } };

export default jordan;
