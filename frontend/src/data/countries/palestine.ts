/**
 * Palestine country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const palestine: Country = { code: 'PS', name: 'Palestine', taxInfo: { standardRate: 16, taxName: 'VAT', currency: 'ILS', region: 'MEA' } };

export default palestine;
