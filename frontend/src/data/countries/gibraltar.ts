/**
 * Gibraltar country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const gibraltar: Country = { code: 'GI', name: 'Gibraltar', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'EU' }};

export default gibraltar;
