/**
 * Saint Lucia country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const saintlucia: Country = { code: 'LC', name: 'Saint Lucia', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'NA' }};

export default saintlucia;
