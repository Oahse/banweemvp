/**
 * Samoa country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const samoa: Country = { code: 'WS', name: 'Samoa', taxInfo: { standardRate: 15, taxName: 'VAT', currency: 'WST', region: 'APAC' } };

export default samoa;
