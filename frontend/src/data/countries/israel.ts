/**
 * Israel country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const israel: Country = { code: 'IL', name: 'Israel', taxInfo: { standardRate: 17, taxName: 'VAT', currency: 'ILS', region: 'MEA' } };

export default israel;
