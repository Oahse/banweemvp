/**
 * Isle of Man country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const isleofman: Country = { code: 'IM', name: 'Isle of Man', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'EU' }};

export default isleofman;
