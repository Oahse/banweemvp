/**
 * Tuvalu country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const tuvalu: Country = { code: 'TV', name: 'Tuvalu', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'APAC' }};

export default tuvalu;
