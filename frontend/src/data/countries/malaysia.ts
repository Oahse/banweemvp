/**
 * Malaysia country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const malaysia: Country = { code: 'MY', name: 'Malaysia', taxInfo: { standardRate: 6, taxName: 'GST', currency: 'MYR', region: 'APAC' } };

export default malaysia;
