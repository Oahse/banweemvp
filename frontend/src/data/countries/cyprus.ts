/**
 * Cyprus country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const cyprus: Country = { code: 'CY', name: 'Cyprus', taxInfo: { standardRate: 19, taxName: 'VAT', currency: 'EUR', region: 'EU' } };

export default cyprus;
