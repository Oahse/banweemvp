/**
 * Finland country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const finland: Country = { code: 'FI', name: 'Finland', taxInfo: { standardRate: 25.5, taxName: 'ALV', currency: 'EUR', region: 'EU' } };

export default finland;
