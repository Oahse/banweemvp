/**
 * Iraq country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const iraq: Country = { code: 'IQ', name: 'Iraq', taxInfo: { standardRate: 15, taxName: 'VAT', currency: 'IQD', region: 'MEA' } };

export default iraq;
