/**
 * Paraguay country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const paraguay: Country = { code: 'PY', name: 'Paraguay', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'LATAM' }};

export default paraguay;
