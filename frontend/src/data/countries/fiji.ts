/**
 * Fiji country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const fiji: Country = { code: 'FJ', name: 'Fiji', taxInfo: { standardRate: 9, taxName: 'VAT', currency: 'FJD', region: 'APAC' } };

export default fiji;
