/**
 * Anguilla country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const anguilla: Country = { code: 'AI', name: 'Anguilla', taxInfo: { taxName: 'No VAT', currency: 'XCD', region: 'NA' }};

export default anguilla;
