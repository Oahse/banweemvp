/**
 * Romania country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const romania: Country = { code: 'RO', name: 'Romania', taxInfo: { standardRate: 19, taxName: 'VAT', currency: 'RON', region: 'EU' } };

export default romania;
