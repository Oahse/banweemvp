/**
 * Greenland country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const greenland: Country = { code: 'GL', name: 'Greenland', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'NA' }};

export default greenland;
