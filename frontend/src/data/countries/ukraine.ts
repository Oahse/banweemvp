/**
 * Ukraine country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const ukraine: Country = { code: 'UA', name: 'Ukraine', taxInfo: { standardRate: 20, taxName: 'VAT', currency: 'UAH', region: 'EU' } };

export default ukraine;
