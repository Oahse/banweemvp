/**
 * Ecuador country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const ecuador: Country = { code: 'EC', name: 'Ecuador', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'LATAM' }};

export default ecuador;
