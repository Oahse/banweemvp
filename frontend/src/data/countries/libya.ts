/**
 * Libya country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const libya: Country = { code: 'LY', name: 'Libya', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'MEA' }};

export default libya;
