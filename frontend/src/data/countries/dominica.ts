/**
 * Dominica country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const dominica: Country = { code: 'DM', name: 'Dominica', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'NA' }};

export default dominica;
