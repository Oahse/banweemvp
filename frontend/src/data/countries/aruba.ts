/**
 * Aruba country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const aruba: Country = { code: 'AW', name: 'Aruba', taxInfo: { standardRate: 6, taxName: 'VAT', currency: 'AWG', region: 'LATAM' } };

export default aruba;
