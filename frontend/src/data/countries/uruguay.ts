/**
 * Uruguay country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const uruguay: Country = { code: 'UY', name: 'Uruguay', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'LATAM' }};

export default uruguay;
