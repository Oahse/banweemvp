/**
 * Indonesia country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const indonesia: Country = { code: 'ID', name: 'Indonesia', taxInfo: { standardRate: 11, taxName: 'VAT', currency: 'IDR', region: 'APAC' } };

export default indonesia;
