/**
 * Mauritania country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const mauritania: Country = { code: 'MR', name: 'Mauritania', taxInfo: { standardRate: 16, taxName: 'VAT', currency: 'MRU', region: 'MEA' } };

export default mauritania;
