/**
 * Tokelau country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const tokelau: Country = { code: 'TK', name: 'Tokelau', taxInfo: { taxName: 'No VAT', currency: 'NZD', region: 'APAC' }};

export default tokelau;
