/**
 * Moldova country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const moldova: Country = { code: 'MD', name: 'Moldova', taxInfo: { standardRate: 20, taxName: 'VAT', currency: 'MDL', region: 'EU' } };

export default moldova;
