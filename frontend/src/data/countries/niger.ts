/**
 * Niger country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const niger: Country = { code: 'NE', name: 'Niger', taxInfo: { standardRate: 19, taxName: 'VAT', currency: 'XOF', region: 'MEA' } };

export default niger;
