/**
 * Maldives country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const maldives: Country = { code: 'MV', name: 'Maldives', taxInfo: { standardRate: 6, taxName: 'GST', currency: 'MVR', region: 'APAC' } };

export default maldives;
