/**
 * Syria country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const syria: Country = { code: 'SY', name: 'Syria', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'MEA' }};

export default syria;
