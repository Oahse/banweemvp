/**
 * Spain country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const spain: Country = { code: 'ES', name: 'Spain', taxInfo: { standardRate: 21, taxName: 'IVA', currency: 'EUR', region: 'EU' } };

export default spain;
