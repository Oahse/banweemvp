/**
 * Saint Helena country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const sainthelena: Country = { code: 'SH', name: 'Saint Helena', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'MEA' }};

export default sainthelena;
