/**
 * Belarus country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const belarus: Country = { code: 'BY', name: 'Belarus', taxInfo: { standardRate: 20, taxName: 'VAT', currency: 'BYN', region: 'EU' } };

export default belarus;
