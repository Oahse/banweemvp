/**
 * Iceland country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const iceland: Country = { code: 'IS', name: 'Iceland', taxInfo: { standardRate: 24, taxName: 'VAT', currency: 'ISK', region: 'EU' } };

export default iceland;
