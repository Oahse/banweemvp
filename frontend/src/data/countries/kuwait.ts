/**
 * Kuwait country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const kuwait: Country = { code: 'KW', name: 'Kuwait', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'MEA' }};

export default kuwait;
