/**
 * Azerbaijan country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const azerbaijan: Country = { code: 'AZ', name: 'Azerbaijan', taxInfo: { standardRate: 18, taxName: 'VAT', currency: 'AZN', region: 'MEA' } };

export default azerbaijan;
