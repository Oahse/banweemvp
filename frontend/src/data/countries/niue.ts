/**
 * Niue country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const niue: Country = { code: 'NU', name: 'Niue', taxInfo: { taxName: 'No VAT', currency: 'NZD', region: 'APAC' }};

export default niue;
