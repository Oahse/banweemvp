/**
 * U.S. Virgin Islands country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const usvirginislands: Country = { code: 'VI', name: 'U.S. Virgin Islands', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'NA' }};

export default usvirginislands;
