/**
 * Italy country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const italy: Country = { code: 'IT', name: 'Italy', taxInfo: { standardRate: 22, taxName: 'IVA', currency: 'EUR', region: 'EU' } };

export default italy;
