/**
 * Slovakia country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const slovakia: Country = { code: 'SK', name: 'Slovakia', taxInfo: { standardRate: 20, taxName: 'VAT', currency: 'EUR', region: 'EU' } };

export default slovakia;
