/**
 * Comoros country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const comoros: Country = { code: 'KM', name: 'Comoros', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'MEA' }};

export default comoros;
