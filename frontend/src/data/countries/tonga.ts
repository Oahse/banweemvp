/**
 * Tonga country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const tonga: Country = { code: 'TO', name: 'Tonga', taxInfo: { standardRate: 15, taxName: 'VAT', currency: 'TOP', region: 'APAC' } };

export default tonga;
