/**
 * Bulgaria country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const bulgaria: Country = { code: 'BG', name: 'Bulgaria', taxInfo: { standardRate: 20, taxName: 'VAT', currency: 'BGN', region: 'EU' } };

export default bulgaria;
