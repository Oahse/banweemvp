/**
 * Kazakhstan country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const kazakhstan: Country = { code: 'KZ', name: 'Kazakhstan', taxInfo: { standardRate: 12, taxName: 'VAT', currency: 'KZT', region: 'APAC' } };

export default kazakhstan;
