/**
 * Kiribati country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const kiribati: Country = { code: 'KI', name: 'Kiribati', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'APAC' }};

export default kiribati;
