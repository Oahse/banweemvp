/**
 * Sweden country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const sweden: Country = { code: 'SE', name: 'Sweden', taxInfo: { standardRate: 25, taxName: 'Moms', currency: 'SEK', region: 'EU' } };

export default sweden;
