/**
 * Antarctica country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const antarctica: Country = { code: 'AQ', name: 'Antarctica', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'Other' }};

export default antarctica;
