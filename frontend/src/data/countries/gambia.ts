/**
 * Gambia country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const gambia: Country = { code: 'GM', name: 'Gambia', taxInfo: { standardRate: 15, taxName: 'VAT', currency: 'GMD', region: 'MEA' } };

export default gambia;
