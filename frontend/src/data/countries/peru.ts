/**
 * Peru country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const peru: Country = { code: 'PE', name: 'Peru', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'LATAM' }};

export default peru;
