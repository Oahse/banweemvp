/**
 * Colombia country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const colombia: Country = { code: 'CO', name: 'Colombia', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'LATAM' }};

export default colombia;
