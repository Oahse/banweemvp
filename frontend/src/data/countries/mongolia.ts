/**
 * Mongolia country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const mongolia: Country = { code: 'MN', name: 'Mongolia', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'APAC' }};

export default mongolia;
