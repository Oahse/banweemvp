/**
 * Czechia country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const czechia: Country = { code: 'CZ', name: 'Czechia', taxInfo: { standardRate: 21, taxName: 'VAT', currency: 'CZK', region: 'EU' } };

export default czechia;
