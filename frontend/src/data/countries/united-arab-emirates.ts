/**
 * United Arab Emirates country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const unitedarabemirates: Country = { code: 'AE', name: 'United Arab Emirates', taxInfo: { standardRate: 5, taxName: 'VAT', currency: 'AED', region: 'MEA' } };

export default unitedarabemirates;
