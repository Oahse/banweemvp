/**
 * Qatar country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const qatar: Country = { code: 'QA', name: 'Qatar', taxInfo: { standardRate: 5, taxName: 'VAT', currency: 'QAR', region: 'MEA' } };

export default qatar;
