/**
 * Sudan country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const sudan: Country = { code: 'SD', name: 'Sudan', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'MEA' }};

export default sudan;
