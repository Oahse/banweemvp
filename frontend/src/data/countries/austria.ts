/**
 * Austria country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const austria: Country = { code: 'AT', name: 'Austria', taxInfo: { standardRate: 20, taxName: 'MwSt', currency: 'EUR', region: 'EU' } };

export default austria;
