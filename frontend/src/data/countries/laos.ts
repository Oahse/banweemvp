/**
 * Laos country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const laos: Country = { code: 'LA', name: 'Laos', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'APAC' }};

export default laos;
