/**
 * Sint Maarten country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const sintmaarten: Country = { code: 'SX', name: 'Sint Maarten', taxInfo: { standardRate: 6, taxName: 'VAT', currency: 'ANG', region: 'LATAM' } };

export default sintmaarten;
