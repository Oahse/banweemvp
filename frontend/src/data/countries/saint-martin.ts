/**
 * Saint Martin country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const saintmartin: Country = { code: 'MF', name: 'Saint Martin', taxInfo: { standardRate: 20, taxName: 'VAT', currency: 'EUR', region: 'LATAM' } };

export default saintmartin;
