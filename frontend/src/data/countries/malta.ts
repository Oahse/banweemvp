/**
 * Malta country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const malta: Country = { code: 'MT', name: 'Malta', taxInfo: { standardRate: 18, taxName: 'VAT', currency: 'EUR', region: 'EU' } };

export default malta;
