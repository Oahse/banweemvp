/**
 * Yemen country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const yemen: Country = { code: 'YE', name: 'Yemen', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'MEA' }};

export default yemen;
