/**
 * Norway country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const norway: Country = { code: 'NO', name: 'Norway', taxInfo: { standardRate: 25, taxName: 'MVA', currency: 'NOK', region: 'EU' } };

export default norway;
