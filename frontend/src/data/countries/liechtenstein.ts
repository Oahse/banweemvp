/**
 * Liechtenstein country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const liechtenstein: Country = { code: 'LI', name: 'Liechtenstein', taxInfo: { standardRate: 7.7, taxName: 'VAT', currency: 'CHF', region: 'EU' } };

export default liechtenstein;
