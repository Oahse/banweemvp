/**
 * Cambodia country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const cambodia: Country = { code: 'KH', name: 'Cambodia', taxInfo: { standardRate: 10, taxName: 'VAT', currency: 'KHR', region: 'APAC' } };

export default cambodia;
