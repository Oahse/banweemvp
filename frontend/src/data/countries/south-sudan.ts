/**
 * South Sudan country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const southsudan: Country = { code: 'SS', name: 'South Sudan', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'MEA' }};

export default southsudan;
