/**
 * Grenada country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const grenada: Country = { code: 'GD', name: 'Grenada', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'NA' }};

export default grenada;
