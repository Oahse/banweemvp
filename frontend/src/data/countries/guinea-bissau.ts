/**
 * Guinea-Bissau country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const guineabissau: Country = { code: 'GW', name: 'Guinea-Bissau', taxInfo: { standardRate: 15, taxName: 'VAT', currency: 'XOF', region: 'MEA' };

export default guineabissau;
