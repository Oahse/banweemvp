/**
 * Latvia country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const latvia: Country = { code: 'LV', name: 'Latvia', taxInfo: { standardRate: 21, taxName: 'VAT', currency: 'EUR', region: 'EU' } };

export default latvia;
