/**
 * Denmark country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const denmark: Country = { code: 'DK', name: 'Denmark', taxInfo: { standardRate: 25, taxName: 'Moms', currency: 'DKK', region: 'EU' } };

export default denmark;
