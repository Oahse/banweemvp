/**
 * Micronesia country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const micronesia: Country = { code: 'FM', name: 'Micronesia', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'APAC' }};

export default micronesia;
