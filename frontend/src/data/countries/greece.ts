/**
 * Greece country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const greece: Country = { code: 'GR', name: 'Greece', taxInfo: { standardRate: 24, taxName: 'VAT', currency: 'EUR', region: 'EU' } };

export default greece;
