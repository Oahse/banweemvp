/**
 * Palau country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const palau: Country = { code: 'PW', name: 'Palau', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'APAC' }};

export default palau;
