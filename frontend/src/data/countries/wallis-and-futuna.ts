/**
 * Wallis and Futuna country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const wallisandfutuna: Country = { code: 'WF', name: 'Wallis and Futuna', taxInfo: { taxName: 'No VAT', currency: 'XPF', region: 'APAC' }};

export default wallisandfutuna;
