/**
 * Taiwan country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const taiwan: Country = { code: 'TW', name: 'Taiwan', taxInfo: { standardRate: 5, taxName: 'VAT', currency: 'TWD', region: 'APAC' } };

export default taiwan;
