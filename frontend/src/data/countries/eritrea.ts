/**
 * Eritrea country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const eritrea: Country = { code: 'ER', name: 'Eritrea', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'MEA' }};

export default eritrea;
