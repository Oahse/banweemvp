/**
 * Seychelles country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const seychelles: Country = { code: 'SC', name: 'Seychelles', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'MEA' }};

export default seychelles;
