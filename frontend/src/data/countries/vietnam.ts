/**
 * Vietnam country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const vietnam: Country = { code: 'VN', name: 'Vietnam', taxInfo: { standardRate: 10, taxName: 'VAT', currency: 'VND', region: 'APAC' } };

export default vietnam;
