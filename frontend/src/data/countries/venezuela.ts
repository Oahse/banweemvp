/**
 * Venezuela country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const venezuela: Country = { code: 'VE', name: 'Venezuela', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'LATAM' }};

export default venezuela;
