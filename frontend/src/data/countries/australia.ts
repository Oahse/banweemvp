/**
 * Australia country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const australia: Country = { code: 'AU', name: 'Australia', taxInfo: { standardRate: 10, taxName: 'GST', currency: 'AUD', region: 'APAC' } };

export default australia;
