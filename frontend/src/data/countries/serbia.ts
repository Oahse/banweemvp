/**
 * Serbia country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const serbia: Country = { code: 'RS', name: 'Serbia', taxInfo: { standardRate: 20, taxName: 'VAT', currency: 'RSD', region: 'EU' } };

export default serbia;
