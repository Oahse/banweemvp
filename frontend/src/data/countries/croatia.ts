/**
 * Croatia country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const croatia: Country = { code: 'HR', name: 'Croatia', taxInfo: { standardRate: 25, taxName: 'VAT', currency: 'EUR', region: 'EU' } };

export default croatia;
