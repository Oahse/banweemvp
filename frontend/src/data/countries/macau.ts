/**
 * Macau country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const macau: Country = { code: 'MO', name: 'Macau', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'APAC' }};

export default macau;
