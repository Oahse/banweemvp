/**
 * Iran country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const iran: Country = { code: 'IR', name: 'Iran', taxInfo: { standardRate: 9, taxName: 'VAT', currency: 'IRR', region: 'MEA' } };

export default iran;
