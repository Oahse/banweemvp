/**
 * Bangladesh country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const bangladesh: Country = { code: 'BD', name: 'Bangladesh', taxInfo: { standardRate: 15, taxName: 'VAT', currency: 'BDT', region: 'APAC' } };

export default bangladesh;
