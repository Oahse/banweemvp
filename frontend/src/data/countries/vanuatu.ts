/**
 * Vanuatu country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const vanuatu: Country = { code: 'VU', name: 'Vanuatu', taxInfo: { standardRate: 12.5, taxName: 'VAT', currency: 'VUV', region: 'APAC' } };

export default vanuatu;
