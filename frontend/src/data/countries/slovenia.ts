/**
 * Slovenia country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const slovenia: Country = { code: 'SI', name: 'Slovenia', taxInfo: { standardRate: 22, taxName: 'VAT', currency: 'EUR', region: 'EU' } };

export default slovenia;
