/**
 * Rwanda country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const rwanda: Country = { code: 'RW', name: 'Rwanda', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'MEA' }};

export default rwanda;
