/**
 * Montenegro country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const montenegro: Country = { code: 'ME', name: 'Montenegro', taxInfo: { standardRate: 21, taxName: 'VAT', currency: 'EUR', region: 'EU' } };

export default montenegro;
