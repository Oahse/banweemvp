/**
 * Chile country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const chile: Country = { code: 'CL', name: 'Chile', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'LATAM' }};

export default chile;
