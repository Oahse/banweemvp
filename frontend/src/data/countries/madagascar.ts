/**
 * Madagascar country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const madagascar: Country = { code: 'MG', name: 'Madagascar', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'MEA' }};

export default madagascar;
