/**
 * Sri Lanka country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const srilanka: Country = { code: 'LK', name: 'Sri Lanka', taxInfo: { standardRate: 15, taxName: 'VAT', currency: 'LKR', region: 'APAC' } };

export default srilanka;
