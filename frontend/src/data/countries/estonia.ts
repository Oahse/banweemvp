/**
 * Estonia country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const estonia: Country = { code: 'EE', name: 'Estonia', taxInfo: { standardRate: 22, taxName: 'VAT', currency: 'EUR', region: 'EU' } };

export default estonia;
