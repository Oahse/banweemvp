/**
 * Nepal country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const nepal: Country = { code: 'NP', name: 'Nepal', taxInfo: { taxName: 'No VAT', currency: 'USD', region: 'APAC' }};

export default nepal;
