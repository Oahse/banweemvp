/**
 * Georgia country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const georgia: Country = { code: 'GE', name: 'Georgia', taxInfo: { standardRate: 18, taxName: 'VAT', currency: 'GEL', region: 'MEA' } };

export default georgia;
