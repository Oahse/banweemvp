/**
 * Bermuda country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const bermuda: Country = { code: 'BM', name: 'Bermuda', taxInfo: { taxName: 'No VAT', currency: 'BMD', region: 'NA' }};

export default bermuda;
