/**
 * Switzerland country data with provinces, cities, and tax information
 */

import { Country } from './index';

export const switzerland: Country = { code: 'CH', name: 'Switzerland', taxInfo: { standardRate: 7.7, taxName: 'MWST', currency: 'CHF', region: 'EU' } };

export default switzerland;
